name: flutter_client_sse_plus
description: 增强版SSE客户端库，提供完善的连接管理、错误处理、重连机制和状态监控
version: 1.0.0
homepage: https://github.com/your-org/flutter_client_sse_plus

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  connectivity_plus: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
# flutter:
# 这里可以添加资源文件
